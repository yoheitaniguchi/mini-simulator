# CLAUDE.md

このファイルはClaude Codeがこのプロジェクトで作業する際に毎回読み込む。簡潔さを優先しているので、
設計判断の根拠や検討の経緯を確認したいときは `docs/design.md`（フル仕様書）を参照すること。

## プロジェクト概要

生産管理（受注〜出荷）のドメイン連携を学ぶための、動くミニマムシミュレーター。
小型コンベア装置（多階層BOM）を題材に、受注を入力すると各ドメイン（受注・マスタ・調達・在庫・生産・出荷）へ
情報が伝播し出荷に至る様子を、日単位のシミュレーション時計で可視化する。

対象読者は開発チームメンバー。商用製品ではなく教材。

## 技術スタック・アーキテクチャ

- React + TypeScript + Vite。**バックエンドサーバーは持たない**（`docs/design.md` §12参照）
- 状態は `useReducer` で一元管理。永続化なし（DBなし）、単一セッション、ページリロードで状態は消える
- ドメインロジックは `src/domain/logic.ts` に純粋関数として実装し、UIから独立させる

## ディレクトリ構成

```
mini-simulator/
├── CLAUDE.md              # このファイル
├── README.md              # 人間向けの概要
├── docs/
│   └── design.md          # フル仕様書（壁打ちで確定した業務ルール・画面設計・演習シナリオ）
├── package.json
├── tsconfig.json
├── vite.config.ts
├── index.html
└── src/
    ├── main.tsx            # エントリポイント
    ├── App.tsx             # 画面本体（現状は動作確認用の最小UI、要作り込み）
    ├── types.ts            # ドメインの型定義（design.md §2, §4, §16）
    ├── data/
    │   └── masterData.ts   # 初期マスタデータ（design.md §17：小型コンベア装置）
    └── domain/
        ├── logic.ts         # ドメインロジック本体（design.md §4〜§8, §16）★最重要ファイル
        └── reducer.ts       # useReducer用reducer。1日の処理順序（design.md §8）を実装
```

将来UIを分割する際は `src/components/` を新設し、`App.tsx` から画面領域ごとにコンポーネントを切り出すこと。

## コマンド

```
npm install
npm run dev       # 開発サーバー起動
npm run build     # 型チェック＋ビルド
npm test          # vitestによる自動テスト実行（§9-1・§9-2の演習シナリオを含む）
```

**`npm install` / ビルド確認・自動テストは実施済み（後述）。** 実装計画の詳細は `docs/implementation-plan.md` を参照。

## 現在の実装状況

- ✅ ドメインロジック（`domain/logic.ts`, `domain/reducer.ts`）：design.md §4〜§8, §16 に基づき実装済み。
  §9-1・§9-2の演習シナリオを自動テスト化して検証済み（`domain/logic.test.ts`）
- ✅ 初期マスタデータ（`data/masterData.ts`）：design.md §17（小型コンベア装置）
- ✅ CI：`.github/workflows/test.yml` でPR発行時に `npm run build`（型チェック）と `npm test` を自動実行
- 🟡 画面：`App.tsx` は受注登録・日送り・受注一覧・材料在庫・出来事ログを備えた最小限の動作確認用UI。
  §9-1のシナリオをブラウザ操作で通しても設計書の日数表と一致することを確認済み。ただし
  design.md §13（メイン画面の完成形）, §14（マスタ画面）, §15（取消確認モーダル）,
  §18（受注一覧ガントチャート）のワイヤーフレーム通りの作り込みは未着手
- ❌ マスタ画面の編集可否の線引き（§14）：reducerのactionは実装済みだが、マスタを編集するUI自体が未実装

## 次にやるべきこと（優先順）

1. `docs/design.md` §13〜§15, §18 に沿って画面を作り込む（現在のApp.tsxは単一ファイルの簡易UI。
   `src/components/` を新設して画面領域ごとに分割することを推奨）
2. マスタ編集画面（§14）を追加し、編集可否の線引き（品目の区分・BOM構造は編集不可、リードタイム・
   BOM員数・仕入先得意先の名称は編集可）をUIに反映する
3. 取消操作の確認モーダル（§15）、受注一覧ガントチャート（§18）を実装する
4. 画面を作り込んだ後も、§9の演習が画面上で同じ結果になることを都度確認する

## 実装時に判明した設計書との差分（要確認）

- **§7の取消ロジック**：仕掛開始時に材料を一括消費する設計（§4）のため、受注済（引当成立前）の取消では
  実際には在庫に「戻す」処理は不要（そもそも何も消費していない）。`logic.ts` の `cancelOrder()` はこれを反映済み
- **受注登録と処理タイミング**：受注は登録した瞬間には評価されず、次の「次の日へ進む」で初めて処理される
  （§8のルール通り、正しい挙動）。§9の演習を画面でなぞる際は、登録直後に1回進める操作を忘れないこと
- **§8の1日の処理順序と日付インクリメントのタイミング**：design.md §8の文面は「1. 日付をD+1進める」を
  最初のステップとして記載しているが、これを文字通り実装すると§9-1・§9-2の演習の日数表（発注の入荷予定日・
  仕掛完成日など）と1日分ずれる。実際に演習の数字を手で検証した結果、`advanceDay()` は「現在の
  `state.day`（＝まだ処理されていない今日）を処理してから、次回呼び出しに備えて日付をD+1進める」という
  順序で実装した。詳細は `logic.ts` の `advanceDay()` のコメントを参照

## コーディング上の注意

- `domain/logic.ts` の関数群は、呼び出し側（`reducer.ts`）が渡した状態のクローンを直接書き換える設計にしている
  （`reducer.ts` 側で `structuredClone` してから渡している）。この層の外側（UI等）からは純粋関数として扱うこと
- BOMの階層探索・所要量計算はすべて `logic.ts` 内の再帰関数（`attemptAllocate`, `computeRequiredTree`）に
  集約している。UI側でBOM階層を独自に辿るロジックを重複させないこと
